Notes: 
Must have the following installed:

OPENSSL
git clone git://git.openssl.org/openssl.git

libssl-dev
sudo apt-get install libssl-dev

GitHub:
https://github.com/jlr84/cs6238-pwd-hardening.git

Command for compiling: 
gcc -Wall openssltest.c -lcrypto

Must install NAG: 
sudo apt-get install nag

Install GMP Library:
sudo apt-get install libgmp3-dev


